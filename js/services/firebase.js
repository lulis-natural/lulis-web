/* ═══════════════════════════════════════════════════════════
   LULIS — services/firebase.js
   Stub de inicialización de Firebase.
   Listo para configurar en Sprint 2.

   INSTRUCCIONES:
   1. Crear proyecto en https://console.firebase.google.com
   2. Ir a Configuración del proyecto → Tu aplicación web
   3. Copiar el objeto firebaseConfig y pegarlo abajo
   4. Descomentar el bloque de inicialización
   5. Agregar el SDK de Firebase en index.html (antes de app.js)
   ═══════════════════════════════════════════════════════════ */

/*
import { initializeApp }    from 'https://www.gstatic.com/firebasejs/10.x.x/firebase-app.js';
import { getFirestore }     from 'https://www.gstatic.com/firebasejs/10.x.x/firebase-firestore.js';
import { getStorage }       from 'https://www.gstatic.com/firebasejs/10.x.x/firebase-storage.js';
import { getAuth }          from 'https://www.gstatic.com/firebasejs/10.x.x/firebase-auth.js';
import { getAnalytics }     from 'https://www.gstatic.com/firebasejs/10.x.x/firebase-analytics.js';

const firebaseConfig = {
  apiKey:            "TU_API_KEY",
  authDomain:        "lulis-app.firebaseapp.com",
  projectId:         "lulis-app",
  storageBucket:     "lulis-app.appspot.com",
  messagingSenderId: "TU_SENDER_ID",
  appId:             "TU_APP_ID",
  measurementId:     "TU_MEASUREMENT_ID",
};

const app       = initializeApp(firebaseConfig);
const db        = getFirestore(app);
const storage   = getStorage(app);
const auth      = getAuth(app);
const analytics = getAnalytics(app);

export { app, db, storage, auth, analytics };
*/

/* Estado actual: Firebase pendiente de configuración */
console.info('LULIS: Firebase pendiente de configuración (services/firebase.js)');
